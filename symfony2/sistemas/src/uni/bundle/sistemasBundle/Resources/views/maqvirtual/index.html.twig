{% extends 'uniSistemasBundle::layout.html.twig' %}

{% block central %} 
    <h1>maqvirtual list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Ipvirtual</th>
                <th>Tareas</th>
                <th>Descripcion</th>
                <th>HW</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.id }}</td>
                <td><a href="{{ path('maqvirtual_show', { 'id': entity.id }) }}">{{ entity.nombre }}</a></td>
                <td>{{ entity.ipvirtual }}</td>
                <td>{{ entity.tareas }}</td>
                <td>{{ entity.descripcion }}</td>
                <td>
                    {{ entity.hardware.nombre }}
                {#<ul>
                    <li>
                        <a href="{{ path('maqvirtual_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('maqvirtual_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>#}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {#    <ul>
        <li>
            <a href="{{ path('maqvirtual_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>#}
    {% endblock %}
